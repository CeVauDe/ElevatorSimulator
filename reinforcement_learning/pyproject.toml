[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["env_tutorial"]

[project]
name = "env_tutorial"
version = "0.0.1"
dependencies = [
  "gymnasium[other]",
  "pygame>=2.1.3",
  "pre-commit",
  "tqdm>=4.67.1",
  "matplotlib>=3.10.7",
  "pyqt6>=6.9.1",
  "ruff>=0.14.2",
]
